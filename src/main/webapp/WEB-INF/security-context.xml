<?xml version="1.0" encoding="UTF-8"?>
<beans:beans xmlns="http://www.springframework.org/schema/security"
             xmlns:beans="http://www.springframework.org/schema/beans"
             xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
             xsi:schemaLocation="http://www.springframework.org/schema/beans
           http://www.springframework.org/schema/beans/spring-beans-4.1.xsd
           http://www.springframework.org/schema/security
           http://www.springframework.org/schema/security/spring-security-4.1.xsd">

    <http auto-config="true" use-expressions="true">
        <intercept-url pattern="/store" access="permitAll"/>
        <intercept-url pattern="/store/order*" access="hasRole('ROLE_USER')"/>
        <intercept-url pattern="/store/delete*" access="hasRole('ROLE_ADMIN')"/>
        <intercept-url pattern="/store/edit*" access="hasRole('ROLE_ADMIN')"/>
        <intercept-url pattern="/store/create" access="hasRole('ROLE_ADMIN')"/>
        <intercept-url pattern="/stat" access="hasRole('ROLE_ADMIN')"/>
        <form-login default-target-url="/store" authentication-failure-url="/login?error=true"/>
        <logout logout-url="/logout" logout-success-url="/store"/>
    </http>
    <authentication-manager>
        <authentication-provider ref="customAuthenticationProvider"/>
            <!--<jdbc-user-service data-source-ref="dataSource"-->
                               <!--users-by-username-query=-->
                                       <!--"SELECT NAME,PASSWORD,ENABLED FROM USERS WHERE NAME=?"-->
                               <!--authorities-by-username-query=-->
                                       <!--"SELECT NAME,ROLE FROM USERS WHERE NAME=?" />-->
            <!--<password-encoder ref="passwordEncoder"/>-->
    </authentication-manager>
    <beans:bean id="passwordEncoder" class="org.springframework.security.crypto.bcrypt.BCryptPasswordEncoder"/>
</beans:beans>